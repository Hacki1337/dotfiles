# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!


# CONFIGURATIONS:

    set $mod mod4
    
    # Font for window titles. Will also be used by the bar unless a different font
    # is used in the bar {} block below.
    font pango:monospace 7
    
    # This font is widely installed, provides lots of unicode glyphs, right-to-left
    # text rendering and scalability on retina/hidpi displays (thanks to pango).
    #font pango:Lucida 7 
    #font pango:FontAwesome Regular 9px
    font pango:monospace 7
    # Before i3 v4.8, we used to recommend this one as the default:
    # font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
    # The font above is very space-efficient, that is, it looks good, sharp and
    # clear in small sizes. However, its unicode glyph coverage is limited, the old
    # X core fonts rendering does not support right-to-left and this being a bitmap
    # font, it doesn’t scale on retina/hidpi displays.
    
    # color for focused container
    client.focused #000000 #000000 #00ff00 #000000 #00000
    
    # Use Mouse+$mod to drag floating windows to their wanted position
    floating_modifier $mod
    
    #focus containers by hovering with the mouse
    focus_follows_mouse no
    
    # specifies wich workspace is shown on wich Display when HDMI3 is connected
    workspace 1 output HDMI3 LVDS1
    workspace 2 output HDMI3 LVDS1
    workspace 3 output HDMI3 LVDS1
    workspace 4 output HDMI3 LVDS1
    workspace 5 output HDMI3 LVDS1
    workspace Web output HDMI3 LVDS1
    workspace 6 output primary
    workspace 7 output primary
    workspace 8 output primary
    workspace 9 output primary
    workspace 10 output primary
    workspace Mail output primary
    workspace xpdf output primary
    workspace ToDo output primary
    workspace Discord output primary
    workspace "VGA" output VGA1
    workspace "HDMI" output HDMI1
    
    # application rules - determine, which Client spawns where and how
            # telegram-desktop
                    assign [class="TelegramDesktop"] → 10
                    for_window [class="TelegramDesktop"] fullscreen
            # org.signal.Signal
                    assign [class="Signal"] → 9
                    for_window [class="Signal"] fullscreen
            # whatsdesk
                    assign [class="whatsdesk"] → 8
                    for_window [class="whatsdesk"] fullscreen
            # spotify
                    # assign [class="Spotify"] → 7
                    for_window [class="Spotify"] move to workspace 7
                    for_window [class="Spotify"] fullscreen
            # thunderbird
                    assign [class="Thunderbird"] → Mail
                    for_window [class="Thunderbird"] fullscreen
                    popup_during_fullscreen smart
            # firefox
                    assign [class="firefox"] → Web
                    popup_during_fullscreen smart
            # brave
                    assign [class="Brave-browser"] → Web
                    popup_during_fullscreen smart
            # keepass
                    assign [class="KeePassXC"] → KeePass
                    for_window [class="KeePassXC"] fullscreen
                    popup_during_fullscreen smart
            # todoist
                    assign [class="Todoist"] → ToDo
                    for_window [class="Todoist"] fullscreen
                    popup_during_fullscreen smart
            # discord
                    assign [class="discord"] → Discord
                    for_window [class="discord"] fullscreen
                    popup_during_fullscreen smart
            # xpdf
                   #assign [class="xpdf"] → xpdf
                   #for_window [class="xpdf"] fullscreen
                   #popup_during_fullscreen smart


# KEYBINDINGS:

    # start a terminal
    bindsym $mod+Return exec xfce4-terminal
    
    # kill focused window
    bindsym $mod+Shift+x kill
    
    # start dmenu (a program launcher)
    bindsym $mod+a exec dmenu_run
    # There also is the (new) i3-dmenu-desktop which only displays applications
    # shipping a .desktop file. It is a wrapper around dmenu, so you need that
    # installed.
    # bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
    
    # change focus
    #bindcode $mod+108+39 focus left
    #bindcode $mod+108+40 focus down
    #bindcode $mod+108+26 focus up
    #bindcode $mod+108+41 focus right
    
    # alternatively, you can use the cursor keys:
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right
    bindsym $mod+Tab focus right
    bindsym $mod+Shift+Tab focus left
    
    # move focused window
    # bindsym $mod+Shift+j move left
    # bindsym $mod+Shift+k move down
    # bindsym $mod+Shift+ move up
    # bindsym $mod+Shift+odiaeresis move right
    
    # alternatively, you can use the cursor keys:
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right
    
    # split in horizontal orientation
    bindsym $mod+s split h
    
    # split in vertical orientation
    bindsym $mod+p split v
    
    # enter fullscreen mode for the focused container
    bindsym $mod+e fullscreen toggle
    
    # change container layout (stacked, tabbed, toggle split)
    bindsym $mod+i layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+l layout toggle split
    
    # toggle tiling / floating
    bindsym $mod+Shift+space floating toggle
    
    # change focus between tiling / floating windows
    bindsym $mod+space focus mode_toggle
    
    #focus the parent container
    #bindsym $mod+a focus parent
    
    # focus the child container
    #bindsym $mod+d focus child
    
    
    # switch to workspace
    bindsym $mod+1 workspace 1
    bindsym $mod+2 workspace 2
    bindsym $mod+3 workspace 3
    bindsym $mod+4 workspace 4
    bindsym $mod+5 workspace 5
    bindsym $mod+6 workspace 6
    bindsym $mod+7 workspace 7
    bindsym $mod+8 workspace 8
    bindsym $mod+9 workspace 9
    bindsym $mod+0 workspace 10
    bindsym $mod+m workspace Mail
    bindsym $mod+v workspace Web
    bindsym $mod+r workspace KeePass
    bindsym $mod+t workspace ToDo
    bindsym $mod+d workspace Discord
    bindsym $mod+odiaeresis workspace xpdf
    bindsym $mod+Ctrl+1 workspace "VGA"
    bindsym $mod+Ctrl+2 workspace "DP"
    bindsym $mod+Ctrl+3 workspace "DVI"
    bindsym $mod+Ctrl+y workspace .
    
    # move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace 1
    bindsym $mod+Shift+2 move container to workspace 2
    bindsym $mod+Shift+3 move container to workspace 3
    bindsym $mod+Shift+4 move container to workspace 4
    bindsym $mod+Shift+5 move container to workspace 5
    bindsym $mod+Shift+6 move container to workspace 6
    bindsym $mod+Shift+7 move container to workspace 7
    bindsym $mod+Shift+8 move container to workspace 8
    bindsym $mod+Shift+9 move container to workspace 9
    bindsym $mod+Shift+0 move container to workspace 10
    bindsym $mod+Shift+Ctrl+1 move container to workspace "VGA"
    bindsym $mod+Shift+Ctrl+2 move container to workspace "DP"
    bindsym $mod+Shift+Ctrl+3 move container to workspace "DVI"
    bindsym $mod+Shift+Ctrl+v move container to workspace "Web"
    bindsym $mod+Shift+Ctrl+y move container to workspace .
    bindsym $mod+Shift+Ctrl+odiaeresis move container to workspace xpdf
    
    # reload the configuration file
    bindsym $mod+Shift+. reload
    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    bindsym $mod+Shift+c restart
    # exit i3 (logs you out of your X session)
    bindsym $mod+Shift+l exec "i3-msg exit"
    
    # make the ThinkPad-Volume-Keys work
    bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
    bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
    bindsym $mod+XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +1%
    bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
    bindsym $mod+XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -1%
    bindsym $mod+XF86AudioMute exec pactl set-sink-volume @DEFAULT_SINK@ 0%
    bindsym XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
    
    # extra-keybindings
    bindsym $mod+Shift+odiaeresis workspace xpdf; workspace 1
    bindsym $mod+Shift+adiaeresis focus right; exec xdotool key ctrl+r; focus left
    bindsym $mod+Shift+v exec brave
    bindsym $mod+Shift+m exec thunderbird
    bindsym $mod+Shift+r exec keepassxc
    bindsym $mod+Shift+t exec todoist
    bindsym $mod+Shift+d exec discord
    bindsym Alt+Shift+n exec keynav
    bindsym Print exec scrot -q 80 'screenshot_%Y-%m-%d-%H:%M:%S.png' -e 'mv $f ~/platte/offtopic/medien/bilder/screenshots'
    bindcode $mod+95 exec rfkill block bluetooth
    bindcode $mod+96 exec sudo hdparm -y /dev/sda
    bindcode $mod+75 exec bash /home/hacki/.scripts/split.sh
    bindcode $mod+76 exec bash /home/hacki/.scripts/split_rev.sh
    #bindsym XF86Launch1 exec cvlc --play-and-exit ~/platte/offtopic/medien/sounds/nice.mp3
    bindcode $mod+133 exec bash /home/hacki/.scripts/changeI3mod4.sh
    bindcode $mod+64 exec bash /home/hacki/.scripts/changeI3mod1.sh
    bindcode $mod+71 exec bash /home/hacki/.scripts/art/art_pidHandler.sh  
    bindcode 160 mode "default"; exec bash ~/.scripts/asdf.sh; exec i3lock -u -e -t -i /home/hacki/platte/offtopic/medien/bilder/my.png -c 000000
    
    # mode-bindings:
    bindsym $mod+c mode "resize"
    #bindcode $mod+49 mode "de"; exec bash /home/hacki/.scripts/uiae.sh
    bindsym XF86Launch1 mode "de"; exec bash /home/hacki/.scripts/uiae.sh

# MODES:

    # resize window (you can also use the mouse for that)
    mode "resize" {
            # These bindings trigger as soon as you enter the resize mode
    
            # Pressing left will shrink the window’s width.
            # Pressing right will grow the window’s width.
            # Pressing up will shrink the window’s height.
            # Pressing down will grow the window’s height.
            #bindsym j resize shrink width 10 px or 10 ppt
            #bindsym k resize grow height 10 px or 10 ppt
            #bindsym l resize shrink height 10 px or 10 ppt
            #bindsym odiaeresis resize grow width 10 px or 10 ppt
    
            # same bindings, but for the arrow keys
            bindsym Left resize shrink width 10 px or 10 ppt
            bindsym Down resize grow height 10 px or 10 ppt
            bindsym Up resize shrink height 10 px or 10 ppt
            bindsym Right resize grow width 10 px or 10 ppt
    
            # back to normal: Enter or Escape
            bindsym Return mode "default"
            bindsym Escape mode "default"
    }
    
    
    
    mode "de" {
        bindcode $mod+49 mode "default"; exec bash /home/hacki/.scripts/asdf.sh
    
        # start a terminal in de
        bindsym $mod+Return exec xfce4-terminal
    
        # kill focused window in de
        bindsym $mod+Shift+q kill
    
        # start dmenu (a program launcher) in de
        bindsym $mod+d exec dmenu_run
        # There also is the (new) i3-dmenu-desktop which only displays applications
        # shipping a .desktop file. It is a wrapper around dmenu, so you need that
        # installed.
        # bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
    
        # change focus in de
        #bindcode $mod+108+39 focus left
        #bindcode $mod+108+40 focus down
        #bindcode $mod+108+26 focus up
        #bindcode $mod+108+41 focus right
        # alternatively, you can use the cursor keys:
        bindsym $mod+Left focus left
        bindsym $mod+Down focus down
        bindsym $mod+Up focus up
        bindsym $mod+Right focus right
        bindsym $mod+Tab focus right
        bindsym $mod+Shift+Tab focus left
    
        # move focused window in de
        # bindsym $mod+Shift+j move left
        # bindsym $mod+Shift+k move down
        # bindsym $mod+Shift+ move up
        # bindsym $mod+Shift+odiaeresis move right
        # alternatively, you can use the cursor keys:
        bindsym $mod+Shift+Left move left
        bindsym $mod+Shift+Down move down
        bindsym $mod+Shift+Up move up
        bindsym $mod+Shift+Right move right
    
        # split in horizontal orientation in de
        bindsym $mod+h split h
    
        # split in vertical orientation in de
        bindsym $mod+v split v
    
        # enter fullscreen mode for the focused container in de
        bindsym $mod+f fullscreen toggle
    
        # change container layout (stacked, tabbed, toggle split) in de
        bindsym $mod+s layout stacking
        bindsym $mod+t layout tabbed
        bindsym $mod+e layout toggle split
    
        # toggle tiling / floating in de
        bindsym $mod+Shift+space floating toggle
    
        # change focus between tiling / floating windows in de
        bindsym $mod+space focus mode_toggle
    
        #focus the parent container in de
        #bindsym $mod+a focus parent
    
        # focus the child container in de
        #bindsym $mod+d focus child
        # switch to workspace
        bindsym $mod+1 workspace 1
        bindsym $mod+2 workspace 2
        bindsym $mod+3 workspace 3
        bindsym $mod+4 workspace 4
        bindsym $mod+5 workspace 5
        bindsym $mod+6 workspace 6
        bindsym $mod+7 workspace 7
        bindsym $mod+8 workspace 8
        bindsym $mod+9 workspace 9
        bindsym $mod+0 workspace 10
        bindsym $mod+m workspace Mail
        bindsym $mod+w workspace Web
        bindsym $mod+k workspace KeePass
        bindsym $mod+l workspace ToDo
        bindsym $mod+odiaeresis workspace Discord
        bindsym $mod+x workspace xpdf
        bindsym $mod+Ctrl+1 workspace "VGA"
        bindsym $mod+Ctrl+2 workspace "DP"
        bindsym $mod+Ctrl+3 workspace "DVI"
    
        # move focused container to workspace
        bindsym $mod+Shift+1 move container to workspace 1
        bindsym $mod+Shift+2 move container to workspace 2
        bindsym $mod+Shift+3 move container to workspace 3
        bindsym $mod+Shift+4 move container to workspace 4
        bindsym $mod+Shift+5 move container to workspace 5
        bindsym $mod+Shift+6 move container to workspace 6
        bindsym $mod+Shift+7 move container to workspace 7
        bindsym $mod+Shift+8 move container to workspace 8
        bindsym $mod+Shift+9 move container to workspace 9
        bindsym $mod+Shift+0 move container to workspace 10
        bindsym $mod+Shift+Ctrl+x move container to workspace xpdf
        bindsym $mod+Shift+Ctrl+1 move container to workspace "VGA"
        bindsym $mod+Shift+Ctrl+2 move container to workspace "DP"
        bindsym $mod+Shift+Ctrl+3 move container to workspace "DVI"
        bindsym $mod+Shift+Ctrl+w move container to workspace "Web"

        # reload the configuration file
        bindcode $mod+Shift+. reload

        # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
        bindsym $mod+Shift+r restart

        # exit i3 (logs you out of your X session)
        bindsym $mod+Shift+e exec "i3-msg exit"
        
        # make the ThinkPad-Volume-Keys work
        bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
        bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
        bindsym $mod+XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +1%
        bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
        bindsym $mod+XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -1%
        bindsym $mod+XF86AudioMute exec pactl set-sink-volume @DEFAULT_SINK@ 0%
        
        # extra-keybindings
        bindsym $mod+Shift+x workspace xpdf; workspace1
        bindsym $mod+Shift+w exec brave
        bindsym $mod+Shift+m exec thunderbird
        bindsym $mod+Shift+k exec keepassxc
        bindsym $mod+Shift+l exec todoist
        bindsym $mod+Shift+odiaeresis exec discord
        bindsym $mod+Shift+j exec keynav
        bindsym Print exec scrot -q 80 'screenshot_%Y-%m-%d-%H:%M:%S.png' -e 'mv $f ~/platte/offtopic/medien/bilder/screenshots'
        bindcode $mod+95 exec rfkill block bluetooth
        bindcode $mod+96 exec sudo hdparm -y /dev/sda
        bindcode $mod+75 exec bash /home/hacki/.scripts/split.sh
        bindcode $mod+76 exec bash /home/hacki/.scripts/split_rev.sh
        #bindsym XF86Launch1 exec cvlc --play-and-exit ~/platte/offtopic/medien/sounds/nice.mp3
        bindcode $mod+133 exec bash /home/hacki/.scripts/changeI3mod4.sh
        bindcode $mod+64 exec bash /home/hacki/.scripts/changeI3mod1.sh
        bindcode $mod+71 exec bash /home/hacki/.scripts/art/art_pidHandler.sh
        bindcode 160 mode "default"; exec bash ~/.scripts/asdf.sh; exec i3lock -u -e -t -i /home/hacki/platte/offtopic/medien/bilder/my.png -c 000000
        
        # mode-bindings:
        bindsym $mod+r mode "resize"
        #bindcode $mod+49 mode "de"; exec bash /home/hacki/.scripts/uiae.sh
        bindsym XF86Launch1 mode "default"; exec bash /home/hacki/.scripts/asdf.sh
    }




# AUTOSTART:

    # Start i3bar to display a workspace bar (plus the system information i3status
    # finds out, if available)
    bar {
            font pango:FontAwesome Regular 9px
            status_command i3status
    }
    
    #exec --no-startup-ud bash xrandr --auto
    exec --no-startup-id nm-applet
    exec --no-startup-id telegram-desktop
    exec --no-startup-id exec signal-desktop-beta
    exec --no-startup-id exec spotify
    exec --no-startup-id compton
    exec --no-startup-id nitrogen --set-centered --head=0 /home/hacki/platte/offtopic/medien/bilder/Trivium.jpg
    exec --no-startup-id whatsdesk
    exec --no-startup-id keepassxc
    exec --no-startup-id blueman-applet
    exec --no-startup-id polychromatic-tray-applet
    exec --no-startup-id todoist
    exec --no-startup-id nextcloud
    #exec --no-startup-id keynav 'loadconfig ~/.config/keynav/keynavrc, deamonize'

    
    # unused:
    #exec --no-startup-ud bash xrandr --output LVDS1 --mode 1366x768
    #exec --no-startup-id nitrogen --set-centered --head=1 /home/hacki/platte/bilder/year-2020-cancelled.jpg
    #exec --no-startup-ud bash xinput --set-prop "SynPS/2 Synaptics TouchPad" 294 0.5
    #exec --no-startup-id thunderbird 
